namespace Morchul.CodeManager
{
    /// <summary>
    /// The InspectionType defines if it is a File and will be blocked when get writing access or if it is plan Text
    /// </summary>
    public enum InspectionType
    {
        FILE,
        TEXT,
    }
}

